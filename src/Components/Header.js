import React from 'react';

const Header = () => {
  return (
    <header className="site-header mo-left header style-1">
      {/* The rest of the header HTML code goes here */}
      {/* ... */}
    </header>
  );
};

export default Header;
